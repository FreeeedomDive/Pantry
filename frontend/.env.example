# Отладка вне Telegram: скопируйте в .env.local и подставьте реальный initData.
# Взять его можно в web-версии Telegram (devtools → sessionStorage → tgWebAppData
# внутри iframe мини-аппа) — подпись должна совпадать с токеном вашего бота.
# Формат: query-строка user=...&chat_instance=...&auth_date=...&hash=...
VITE_DEBUG_INIT_DATA=
