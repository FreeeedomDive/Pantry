spring:
  application:
    name: pantry-migrator
  main:
    web-application-type: none
    banner-mode: "off"
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
