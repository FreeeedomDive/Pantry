services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: pantry
      POSTGRES_USER: pantry
      POSTGRES_PASSWORD: pantry
    ports:
      - "55432:5432"
    volumes:
      - pantry_pgdata:/var/lib/postgresql/data

volumes:
  pantry_pgdata:
