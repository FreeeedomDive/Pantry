name: Detect Changes

on:
  workflow_call:
    inputs:
      base-ref:
        description: 'Branch to diff the whole current ref against (e.g. master). Empty = last commit only.'
        type: string
        required: false
        default: ''
    outputs:
      backend:
        description: "Backend modules or shared build files changed"
        value: ${{ jobs.detect.outputs.backend }}
      frontend:
        description: "Frontend changed"
        value: ${{ jobs.detect.outputs.frontend }}
      frontend_deploy:
        description: "Deployable frontend application files changed"
        value: ${{ jobs.detect.outputs.frontend_deploy }}
      migrate:
        description: "Schema, migrator or shared build files changed"
        value: ${{ jobs.detect.outputs.migrate }}
      nginx:
        description: "Nginx changed"
        value: ${{ jobs.detect.outputs.nginx }}
      e2e:
        description: "E2E-relevant application or infrastructure changed"
        value: ${{ jobs.detect.outputs.e2e }}

jobs:
  detect:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.check.outputs.backend }}
      frontend: ${{ steps.check.outputs.frontend }}
      frontend_deploy: ${{ steps.check.outputs.frontend_deploy }}
      migrate: ${{ steps.check.outputs.migrate }}
      nginx: ${{ steps.check.outputs.nginx }}
      e2e: ${{ steps.check.outputs.e2e }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed paths
        id: check
        env:
          BASE_REF: ${{ inputs.base-ref }}
        run: |
          if [ -n "$BASE_REF" ]; then
            git fetch --no-tags origin "+refs/heads/$BASE_REF:refs/remotes/origin/$BASE_REF"
            BASE=$(git merge-base "origin/$BASE_REF" HEAD)
          else
            BASE="HEAD^"
          fi

          CHANGED=$(git diff --name-only "$BASE" HEAD)

          backend=false
          frontend=false
          frontend_deploy=false
          migrate=false
          nginx=false
          e2e=false

          # Schema migrations or migrator module -> rebuild migrator
          if echo "$CHANGED" | grep -qE '^(schema|migrator)/|^Dockerfile\.migrator$'; then
            migrate=true
          fi

          # Backend modules -> rebuild backend
          if echo "$CHANGED" | grep -qE '^(domain|application|infrastructure|bootstrap)/|^Dockerfile$'; then
            backend=true
          fi

          # Shared Gradle build files -> rebuild both backend and migrator
          if echo "$CHANGED" | grep -qE '^(buildSrc|gradle)/|^(settings|build)\.gradle\.kts$|^gradlew(\.bat)?$'; then
            backend=true
            migrate=true
          fi

          # Any frontend change -> run frontend checks
          if echo "$CHANGED" | grep -qE '^frontend/'; then
            frontend=true
          fi

          # Frontend tests and Playwright configuration require checks, but not a production deploy.
          if echo "$CHANGED" | grep -E '^frontend/' | grep -qEv '^frontend/(e2e/|playwright\.config\.ts$|.*/__tests__/|.*\.(test|spec)\.(ts|tsx|js|jsx)$)'; then
            frontend_deploy=true
          fi

          # Nginx -> rebuild nginx
          if echo "$CHANGED" | grep -qE '^nginx/'; then
            nginx=true
          fi

          # E2E covers the complete frontend/backend path plus its own infrastructure.
          if [ "$backend" = true ] || [ "$frontend" = true ] || [ "$migrate" = true ] || [ "$nginx" = true ]; then
            e2e=true
          fi
          if echo "$CHANGED" | grep -qE '^(compose\.e2e\.yaml|scripts/run-e2e\.sh|\.github/workflows/(E2E|Checks|DetectChanges)\.yml)$'; then
            e2e=true
          fi

          {
            echo "backend=$backend"
            echo "frontend=$frontend"
            echo "frontend_deploy=$frontend_deploy"
            echo "migrate=$migrate"
            echo "nginx=$nginx"
            echo "e2e=$e2e"
          } >> "$GITHUB_OUTPUT"
