// Root project intentionally has no plugins applied.
// Common configuration lives in buildSrc convention plugins:
//   pantry.kotlin-common, pantry.spring-conventions, pantry.jpa-conventions, pantry.testing
// Apply them per-module in each subproject's build.gradle.kts.
